name: Testing

on: [push]

jobs:
  test:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@master
      with:
        submodules: true
    - name: Test Shell Common Functions Library
      run: ./run-tests.sh
